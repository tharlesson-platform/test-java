version: '3.8'
services:
  db:
    image: postgres:13
    container_name: taskflowpro-db
    environment:
      POSTGRES_DB: taskflowpro
      POSTGRES_USER: taskflowuser
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
  app:
    build: .
    container_name: taskflowpro-app
    depends_on:
      - db
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://db:5432/taskflowpro
      SPRING_DATASOURCE_USERNAME: taskflowuser
      SPRING_DATASOURCE_PASSWORD: password